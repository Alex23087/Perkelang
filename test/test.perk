import "stdio.h"
extern printf : (char*, ...) -> int

fun main(): int {
    let x := 42;

    let test := (): int => {
        printf("test %d\n", 696969);
    };
    
    test();

    sum(1, 2);

    return 0;
}

fun sum (a: int, b: int): int {
    return a + b;
}